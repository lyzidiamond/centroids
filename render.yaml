services:
  - type: web
    name: centroids
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn hello:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.2
